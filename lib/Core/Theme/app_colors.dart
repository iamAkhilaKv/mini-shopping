import 'package:flutter/material.dart';

class AppColors {

  // Basic
  static const white = Colors.white;
  static const black = Colors.black;

  // Scaffold / Background
  static const background = Color(0xFFE6EADF);
  static const scaffold = white;

  // Brand
  static const primary = Color(0xFF8FA77A);
  static const primaryDark = Color(0xFF7E946B);

  // Surfaces
  static const card = Color(0xFFF5F3EE);
  static const surface = Color(0xFFE0E0E0);

  // Text
  static const textPrimary = Color(0xFF000500);
  static const textSecondary = Color(0xFF8A8A8A);

  // Accent
  static const price = Color(0xFF7FAE5A);
  static const wishlist = Color(0xFFF28B9C);

}